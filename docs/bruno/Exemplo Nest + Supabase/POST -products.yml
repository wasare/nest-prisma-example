info:
  name: POST /products
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/products"
  body:
    type: json
    data: |-
      {
        "name": "Pizza Calabresa Grande",
        "price": 59.90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
